services:
  web:
    image: nginx:1.27-alpine
    container_name: homelab-hermes-desktop-bridge_web_1
    restart: always
    ports:
      - "18791:8080"
    volumes:
      - ${APP_DATA_DIR}/nginx.conf:/etc/nginx/conf.d/default.conf:ro
